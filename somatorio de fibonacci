#include <stdio.h>

int main () {
    int n1=1, n2=2, n3, somatorio=0;
    while (n1<=4000000) {
        if (n1%2==0) {
            somatorio += n1;
        }
        n3=n1+n2;
        n1=n2;
        n2=n3;
    }
    printf("%d", somatorio);
    return 0;
}
